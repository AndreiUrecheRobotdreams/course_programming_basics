abstract class Vehicle
{
    public int Id { get; set; }

    public Vehicle(int id)
    {
        Id = id;
    }

    // Abstraktní metoda
    public abstract void DisplayDetails();

    // Virtuální metoda
    public virtual void StartEngine()
    {
        Console.WriteLine($"Vehicle {Id} engine started.");
    }
}
